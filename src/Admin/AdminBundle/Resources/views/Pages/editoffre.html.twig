
{%extends  '::base.html.twig'%}

{% block body%}
    <br><br>

     <div class="content">
      <!-- Start: SERVICE LIST -->
        <div class="container">
          <div class="page-header">
              
             
              <h2><small><font color="black"><font size=+2><u>Espace Admin</u></font></font></small></h2>
          </div>
                
          <div class="span3">{% include('AdminBundle:Modules:menuadmin.html.twig')%}</div>
           <div class="row-fluid">
               <div class="span9">
                   <center><h1><small> Modifier une offre</small></h1></center>
                   
                       <br> 
          {% for flashMessage in app.session.flashbag.get('success') %}
              <div class="form-row"><font color="green">offre modifiée</font></div>
{% endfor %}
                  {% for flashMessage in app.session.flashbag.get('error') %}
              <div class="form-row"><font color="red">Erreur modification de l'offre! </font></div>
{% endfor %}
                   <div>

                       <form action="{{path('admin_editoffre',{'idoffre':idoffre})}}" method="POST" class="form-signin-signup">
    {# affiche l'unique champ de la tâche : description #}
  
   
    {{form_widget(addoffre)}}
    <input type="submit" value="enregistrer" class="btn btn-primary">
    </form>
 
</di>
           </div>
          </div>
        </div></div>
      <!-- End: SERVICE LIST -->
    </div>
     {%endblock%} 
    
    
    
    
           
        
