{%extends  '::base.html.twig'%}

{% block body%}
    <br><br>

     <div class="content">
      <!-- Start: SERVICE LIST -->
        <div class="container">
          <div class="page-header">
                            <h2><small><font color="black"><font size=+2><u>Espace Candidat</u></font></font></small></h2>
          </div>
                
          <div class="span3">{% include('CandidatBundle:Modules:menucandidat.html.twig')%}</div>
           <div class="row-fluid">
              
          <div class="span9" class="icon-align-left">BINEVENU(E) {{app.user.username|upper}} DANS VOTRE ESPACE!
              <br> <br>
               {% for flashMessage in app.session.flashbag.get('notice') %}
              <div class="form-row"><font color="green">Compétence modifiée</font></div>
{% endfor %}
                  {% for flashMessage in app.session.flashbag.get('erreur') %}
              <div class="form-row"><font color="red">Erreur de modification</font></div>
{% endfor %}
                
              <form method="POST" action="{{path('candidat_editcompetence',{'id':id})}}" class="form-signin-signup">
{{form_label(form.nomCompetence,'Enumerez vos compétences:')}}
                      {{form_widget(form.nomCompetence)}}
{{form_label(form.idDomaineCompetence,"Séléctionnez le domaine")}}
{{form_widget(form.idDomaineCompetence)}}
        {{form_rest(form)}}
                      <button type="submit"  class="btn-primary btn">modifier</button>
    </form>

           </div>
          </div>
        </div>
   
  
     {%endblock%} 
    
    
    
    
    