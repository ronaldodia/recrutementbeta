




{%extends  '::base.html.twig'%}

{% block body%}
    <br><br>

     <div class="content">
      <!-- Start: SERVICE LIST -->
        <div class="container">
          <div class="page-header">
                            <h2><small><font color="black"><font size=+2><u>Espace Candidat</u></font></font></small></h2>
          </div>
                
          <div class="span3">{% include('CandidatBundle:Modules:menucandidat.html.twig')%}</div>
           <div class="row-fluid">
              
          <div class="span9" class="icon-align-left">BINEVENU(E) {{app.user.username|upper}} DANS VOTRE ESPACE!
              <br> <br>
              {% for flashMessage in app.session.flashbag.get('msgpasetatcivile') %}
              <div class="form-row"><font color="green">Vous devriez créer votre CV avant de pouvoir postuler!</font></div>
{% endfor %}
              {% for flashMessage in app.session.flashbag.get('notice') %}
              <div class="form-row"><font color="green">Etat civile mis à jour, vous pouvez cliquer sur le bouton 'Etat civile à votre gauche</font></div>
{% endfor %}
                  {% for flashMessage in app.session.flashbag.get('erreur') %}
              <div class="form-row"><font color="red">Vous devriez modifier ce que vous avez ajouté, vous avez déjà un etat civile</font></div>
{% endfor %}
                  <form method="POST" action="{{path('candidat_add')}}" class="form-contact-us" {{ form_enctype(addcandidatform)}}>
{{form_widget(addcandidatform)}}
                      <button type="submit"  class="btn-primary btn">ajouter</button>
    </form>

           </div>
          </div>
        </div>
   
  
     {%endblock%} 
    
    
    
    
    