{% trans_default_domain 'FOSUserBundle' %}




{%extends  '::base.html.twig'%}

{% block body%}
    <br><br>

     <div class="content">
      <!-- Start: SERVICE LIST -->
        <div class="container">
          <div class="page-header">
                            <h2><small><font color="black"><font size=+2><u>Changer le mot de passe</u></font></font></small></h2>
          </div>
                
          <div class="span3">{%if is_granted("ROLE_USER")  %}

              {%if is_granted("ROLE_MANAGER")%}
                    {%if is_granted("ROLE_ADMIN")%}
                       {% include('AdminBundle:Modules:menuadmin.html.twig')%}

                    {%else%}
                      {% include('EntrepriseBundle:Modules:menuentreprise.html.twig')%}
                    {%endif%}

                 {%else%}
                       {% include('CandidatBundle:Modules:menucandidat.html.twig')%}
                        
                {%endif%}


         {%endif%}</div>
           <div class="row-fluid">
              
          <div class="span9" class="icon-align-left">BINEVENU(E) {{app.user.username|upper}} DANS VOTRE ESPACE!
              <br> <br>
   {% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_change_password') }}" {{ form_enctype(form) }} method="POST" class="form-signin-signup">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'change_password.submit'|trans }}" class="btn btn-primary" />
    </div>
</form>


           </div>
          </div>
        </div>
      <!-- End: SERVICE LIST -->
  
     {%endblock%} 
    
    
    
    
    