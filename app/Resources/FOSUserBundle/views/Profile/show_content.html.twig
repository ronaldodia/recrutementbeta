


{%extends  '::base.html.twig'%}

{% block body%}
    <br><br>

     <div class="content">
      <!-- Start: SERVICE LIST -->
        <div class="container">
          <div class="page-header">
                            <h2><small><font color="black"><font size=+2><u>Profile</u></font></font></small></h2>
          </div>
                
          <div class="span3">
              
              
              
               {%if is_granted("ROLE_USER")  %}

              {%if is_granted("ROLE_MANAGER")%}
                    {%if is_granted("ROLE_ADMIN")%}
                     {% include('AdminBundle:Modules:menuadmin.html.twig')%}

                    {%else%}
                      {% include('EntrepriseBundle:Modules:menuentreprise.html.twig')%}
                    {%endif%}

                 {%else%}
                       {% include('CandidatBundle:Modules:menucandidat.html.twig')%}
                        
                {%endif%}


         {%endif%}
              
              

</div>
           <div class="row-fluid">
              
          <div class="span9" class="icon-align-left">BINEVENU(E) {{app.user.username|upper}} DANS VOTRE ESPACE!
              <br> <br>
             {% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <div class="row">
            <div class="page-header">
                <h1><small>{{ 'profile.show.username'|trans }}: </small></h1> <font size=+2> {{ user.username }}</font>
            </div>
          </div>
    <p></p>
    <div class="row">
            <div class="page-header">
                <h1><small>{{ 'profile.show.email'|trans }}:</small></h1> <font size=+2> {{ user.email }}</font>
            </div>
          </div>
    <a href="{{path('fos_user_profile_edit')}}"><img src="{{asset('img/edit.png')}}" width="35" height="35" align="left"></a>
</div>
           </div>
          </div>
        </div>
      <!-- End: SERVICE LIST -->
  
     {%endblock%} 
    
    
    
    
    